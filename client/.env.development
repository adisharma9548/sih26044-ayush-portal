# =================================================================
# Client Environment Configuration (Local Development)
# Vite automatically loads this when running `npm run dev` (mode: development)
# =================================================================

# Backend API endpoint URL (Express REST API)
VITE_API_URL=http://localhost:5000/api

# Backend Base URL (for Socket.IO real-time video signaling & WebSockets)
VITE_BACKEND_URL=http://localhost:5000

# App Branding & Support
VITE_APP_NAME=AYUSH Portal
VITE_SUPPORT_EMAIL=support@ayushportal.in
